import { DateTimeFormatePipe } from './date-time-formate.pipe';

describe('DateTimeFormatePipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeFormatePipe();
    expect(pipe).toBeTruthy();
  });
});
